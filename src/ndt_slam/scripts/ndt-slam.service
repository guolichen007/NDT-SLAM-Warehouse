# Deprecated compatibility pointer. Install generated units with:
#   rosrun ndt_slam install_server_services.sh --workspace PATH --user USER --data-root PATH
#
# The executable service template is scripts/ops/ndt-slam.service.in. Keeping
# this non-installable stub prevents old deployments from silently retaining
# the former inverted ExecStartPre flock check.
